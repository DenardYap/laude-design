export interface EmptyCanvasProps {
  /** Override the default invitation copy. */
  message?: string;
}

export interface FilesActionsProps {
  projectId: string;
}

export interface Point {
  x: number;
  y: number;
}
